{
  "name": "payment-switch-application",
  "version": "1.0.0",
  "description": "Enterprise Payment Switch Application - Central hub for routing and processing financial transactions",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "setup": "npm run install:all",
    "install:all": "npm install && npm run install:services && npm run install:frontend",
    "install:services": "cd services/transaction-processor && npm install && cd ../routing-engine && npm install && cd ../psp-gateway && npm install && cd ../management-api && npm install && cd ../monitoring && npm install",
    "install:frontend": "cd frontend && npm install"
  },
  "keywords": [
    "payment",
    "switch",
    "fintech",
    "transaction-processing",
    "psp",
    "iso8583",
    "iso20022",
    "feathers",
    "mongodb",
    "node-red"
  ],
  "author": "Payment Switch Development Team",
  "license": "PROPRIETARY",
  "dependencies": {
    "dotenv": "^16.3.1",
    "winston": "^3.11.0",
    "helmet": "^7.1.0",
    "cors": "^2.8.5",
    "express-rate-limit": "^7.1.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.7.0",
    "supertest": "^6.3.3",
    "eslint": "^8.56.0",
    "prettier": "^3.1.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}

